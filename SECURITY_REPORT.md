# التقرير الأمني — Networkat SD-WAN (core/web_app)

**تاريخ الفحص:** 2026-09-15
**النطاق:** كامل الكود المتتبّع في هذا الـ repo (`core/web_app/`) — التطبيقين Flask، بوابة FastAPI، مهمة الـ heartbeat، القوالب، والاعتمادات.
**المنهجية:** فحص يدوي للملفات الحساسة (الإعدادات، المصادقة، الجيت واي) + فحص آلي عبر grep لكامل الشجرة (حقن SQL/أوامر، أسرار مكتوبة صراحة، CSRF، صلاحيات الجلسة، XSS، IDOR).

---

## ملخص تنفيذي

النتيجة الأخطر بلا منازع: **مفتاح المصادقة الداخلي (`INTERNAL_API_KEY`) الذي يحمي بوابة FastAPI بالكامل — وهي نقطة الاتصال الوحيدة مع NetBird وAdGuard وأجهزة الـ edge — مكتوب صراحة كنص ثابت (hardcoded) في 9 ملفات مختلفة (11 موضعًا)**، بدلاً من قراءته من متغيرات البيئة فقط. أي شخص يملك حق قراءة الكود (أو حتى يفتح صفحة `/docs` العامة) يملك فعليًا تحكمًا كاملًا في الشبكة.

بجانب ذلك: تشفير كلمات المرور ضعيف (SHA-256 بدل bcrypt رغم وجوده في الاعتمادات)، لا يوجد حماية CSRF رغم توفر Flask-WTF، لا يوجد Rate Limiting على صفحات الدخول، وكلمة مرور بريد إلكتروني حقيقية مكتوبة كنص احتياطي في موضعين من الكود.

**لا يوجد ملف `.env` مرفوع على git** (نقطة إيجابية مؤكدة)، ولا توجد ثغرات حقن SQL أو حقن أوامر في الكود، ولا يوجد رفع ملفات أو تقديم ملفات ثابتة ديناميكي قابل للاستغلال — طبقة الوصول للبيانات نظيفة تقنيًا.

| الخطورة | العدد |
|---|---|
| 🔴 حرجة (Critical) | 2 |
| 🟠 عالية (High) | 5 |
| 🟡 متوسطة (Medium) | 3 |
| 🟢 منخفضة/معلومة (Low/Info) | 6 |

---

## 🔴 حرجة (Critical)

### 1. مفتاح المصادقة الداخلي مكتوب صراحة في 9 ملفات (11 موضعًا)
القيمة الثابتة `57e443f0625abfa313425a020626b078899d4db7ff8d09d59c5f7ae4d0c6d874` — وهي في نفس الوقت `INTERNAL_API_KEY` (بوابة FastAPI) وtoken الوصول لـ NetBird API — موجودة كنص صريح في القائمة الكاملة التالية (تم التحقق منها عبر grep شامل على كامل الملفات المتتبّعة في git):

1. `fastapi_app/dependencies.py:7` — القيمة الفعلية المستخدمة في `verify_api_key()`؛ سطر `os.getenv("INTERNAL_API_KEY")` معلّق (commented out) فوقها مباشرة، يعني متغير البيئة **معطّل فعليًا** بغض النظر عن قيمته في `.env`.
2. `fastapi_app/main.py:35` — نفس القيمة مدرجة داخل `license_info` في تعريف تطبيق FastAPI، وتظهر بشكل علني لأي زائر لصفحة `/docs`.
3. `app.py:46` — كـ fallback داخل `os.environ.get('NETBIRD_API_TOKEN', '57e443f0...')`.
4. `admin/routes.py:49` — داخل دالة `_get_api_headers()`.
5. `services/netbird_service.py:36` — داخل `get_api_headers()`.
6. `services/customer_service.py:114`
7. `services/customer_service.py:180`
8. `services/customer_service.py:441`
9. `services/edge_service.py:95`
10. `services/edge_service.py:265`
11. `services/subscription_service.py:27` — `self.api_key = os.getenv("INTERNAL_API_KEY", "57e443f0...")` — نفس القيمة كـ fallback؛ ستُفعَّل تلقائيًا وبصمت إذا غاب متغير البيئة.

**الأثر:** هذا التوكن هو حدود الثقة الوحيدة (trust boundary) بين التطبيقين Flask وبوابة FastAPI التي تتحكم في NetBird وAdGuard وتُصدر أوامر مباشرة لأجهزة الـ edge. تسريبه (عبر الكود، أو `/docs` العامة) يعني تحكمًا كاملًا في الشبكة بأكملها من أي طرف خارجي، بغض النظر عن أي تدوير (rotation) لاحق للمفتاح في `.env` — لأن القيمة القديمة ستبقى تعمل من هذه المواضع التسعة.

**التوصية:** توليد مفتاح جديد فورًا، حذف كل القيم الثابتة من الملفات التسعة (11 موضعًا) والاعتماد حصريًا على `os.getenv("INTERNAL_API_KEY")` بدون قيمة افتراضية (فشل صريح إن لم يوجد المتغير)، وإزالته من `license_info` نهائيًا. اعتبار المفتاح الحالي **مخترقًا (compromised)** لأنه كان متاحًا لأي شخص برؤية الكود أو `/docs`. **تنبيه لأي عملية إصلاح لاحقة:** تدوير المفتاح مع تصحيح 8 ملفات فقط (تجاهل `subscription_service.py`) سيؤدي لإعادة تفعيل المفتاح المسرّب صامتًا بمجرد غياب متغير البيئة.

### 2. تشفير كلمات المرور ضعيف (SHA-256 بدل bcrypt)
`utils/password.py`:
```python
def hash_password(password):
    salt = secrets.token_hex(16)
    hash_value = hashlib.sha256((salt + password).encode()).hexdigest()
    return f"{salt}:{hash_value}"
```
هذه هي الدالة الوحيدة الفعلية المستخدمة لتخزين كلمات مرور المستخدمين الحقيقيين (`repositories/user_repository.py`، `services/auth_service.py` عند إنشاء الأدمن الافتراضي). SHA-256 مع تكرار واحد (iteration) سريع جدًا وقابل للكسر بالقوة الغاشمة عبر GPU/ASIC رغم وجود الملح (salt) العشوائي — وهذا بالضبط ما توجد خوارزميات مثل bcrypt/argon2/scrypt لمنعه (تباطؤ متعمد + معامل عمل قابل للضبط).

المفارقة: **`bcrypt==5.0.0` موجودة في `requirements.txt` لكنها غير مستخدمة إطلاقًا في الكود** — تبعية ميتة لا تحمي شيئًا.

**التوصية:** الانتقال إلى `bcrypt` أو `argon2-cid` لتشفير كلمات المرور، مع خطة ترحيل تدريجي (rehash عند أول تسجيل دخول ناجح لكل مستخدم).

---

## 🟠 عالية (High)

### 3. غياب أي حماية من CSRF رغم توفر المكتبة
`Flask-WTF==1.3.0` و`WTForms==3.2.2` مدرجتان في `requirements.txt`، لكن لا يوجد أي استدعاء لـ `CSRFProtect(app)` في `app.py` أو `client_app.py` أو `extensions.py`، ولا أي `csrf_token()` في القوالب. الإثبات الوحيد على أن الفريق كان واعيًا لهذا هو سطر في اختبار: `tests/test_peer_update_api.py:15: self.app.config['WTF_CSRF_ENABLED'] = False`.

كل نقاط POST/PUT/DELETE المُغيّرة للحالة (قواعد الجدار الناري، قواعد DNS، إدارة الموظفين، حذف عميل، تغيير الاشتراك) تعتمد فقط على كوكي الجلسة — أي موقع خارجي خبيث يمكنه تنفيذ طلبات باسم مستخدم/أدمن مسجّل دخول بدون علمه.

**التوصية:** تفعيل `CSRFProtect` عالميًا على كلا تطبيقي Flask، وإضافة `csrf_token()` في كل نموذج.

### 4. لا يوجد Rate Limiting على صفحات الدخول الأساسية
دالة `authenticate()` في `services/auth_service.py` (تخدم كلًا من دخول الأدمن ودخول العميل) لا تحتوي على أي تحديد لعدد المحاولات أو تأخير أو قفل حساب — عدد غير محدود من محاولات كلمة المرور. بالمقابل، `fastapi_app/routes/auth.py` (تسجيل دخول تركيب الأجهزة المحمولة) **ينفّذ فعليًا** حماية `_is_rate_limited()` — أي أن الفجوة مقصورة تحديدًا على أكثر نقطتين عرضة للاستخدام من متصفح عام: دخول الأدمن ودخول العميل.

مقترنة بضعف خوارزمية التشفير (البند 2)، هذا يخفّض كثيرًا تكلفة هجوم تخمين كلمات المرور (brute-force / credential stuffing).

**التوصية:** إضافة `Flask-Limiter` (غير موجودة حاليًا في `requirements.txt`) على `/login` في كلا التطبيقين، وقفل مؤقت للحساب بعد عدد محاولات فاشلة.

### 5. أكواد التحقق (OTP) غير عشوائية بأمان تشفيري
`client/routes/auth.py` يستخدم `random.randint(100000, 999999)` لتوليد أكواد التحقق من البريد وإعادة تعيين كلمة المرور. وحدة `random` في بايثون **ليست** مولّد أرقام عشوائي آمن تشفيريًا (CSPRNG) — الحالة الداخلية قابلة للتنبؤ إذا عُرفت مخرجات كافية. الأخطر أنه لا يوجد أي تحديد لعدد محاولات إدخال الكود في `verify_email`/`reset_password` — كود من 6 أرقام (مليون احتمال) بدون rate limiting قابل للتخمين خلال دقائق.

**التوصية:** استخدام `secrets.randbelow()` أو `secrets.token_hex()`، مع تحديد صارم لعدد محاولات إدخال الكود لكل جلسة (مثلاً 5 محاولات ثم إبطال الكود).

### 6. كلمة مرور بريد إلكتروني حقيقية مكتوبة صراحة في الكود (في موضعين)
```python
MAIL_PASSWORD = os.getenv("MAIL_PASSWORD", "bePositive\"2006\"")
```
موجودة في **موضعين مستقلين**: `config/settings.py` و`utils/email.py:29` (نفس القيمة بالضبط، لمستخدم `no_reply@networkat.net`، `email.py:27`). كلمة مرور فعلية تبدو حقيقية (وليست placeholder) مستخدمة كقيمة احتياطية، متتبّعة في تاريخ git. أي شخص برؤية الكود أو تاريخ الـ commits يملك بيانات دخول هذا البريد.

**التوصية:** تغيير كلمة مرور هذا الحساب فورًا، وإزالة القيمة الافتراضية من كلا الموضعين (فشل صريح عند غياب `MAIL_PASSWORD`).

### 6ب. احتمال حقن رؤوس بريد إلكتروني (Email Header Injection)
`client/routes/auth.py:78` يقرأ `client_email = request.form.get('client_email', '').strip()` بدون أي تحقق من صيغة البريد (رغم وجود مكتبة `email-validator` أصلًا في `requirements.txt` وعدم استخدامها هنا)، ثم يُمرَّر مباشرة إلى `send_verification_email`/`send_password_reset_email` في `utils/email.py`، حيث يُستخدم كما هو في `msg["To"] = to_email` (الأسطر 43، 89، 137) دون تعقيم من محارف `\r\n`. قيمة بريد مصمَّمة خصيصًا تحتوي على newline يمكنها نظريًا حقن رؤوس MIME/SMTP إضافية (مثل مستقبلين إضافيين عبر BCC).

**التوصية:** التحقق الصارم من صيغة البريد عبر `email-validator` (الموجودة أصلًا) قبل أي استخدام، ورفض أي قيمة تحتوي على أسطر جديدة.

### 7. مجلد `/scripts` العام مُقدَّم دون أي مصادقة — نمط تعرّض قائم
`fastapi_app/main.py:46`: `app.mount("/scripts", StaticFiles(directory=_SCRIPTS_DIR), name="scripts")` — هذا الـ mount موجود **خارج** التبعية الأمنية `Depends(verify_api_key)` المطبّقة على كل مسار آخر في التطبيق. حاليًا يحتوي المجلد على ملف واحد فقط (`install.sh`، سكريبت فحص اتصال بسيط بدون أسرار)، لكن أي ملف يُوضع مستقبلًا في `fastapi_app/scripts/` (سكريبت تركيب حقيقي، ملف تهيئة، إلخ) سيُنشر تلقائيًا للإنترنت بدون أي مصادقة، خلافًا لبقية النظام بالكامل.

**التوصية:** توثيق هذا الاستثناء بوضوح في الكود (تعليق تحذيري)، والتأكد أن أي ملف يُضاف لهذا المجلد مُراجَع كمحتوى عام قبل النشر.

---

## 🟡 متوسطة (Medium)

### 8. إعدادات كوكي الجلسة غير مضبوطة
لا يوجد في `config/settings.py` أي تعيين لـ `SESSION_COOKIE_SECURE`، `SESSION_COOKIE_SAMESITE`، أو `PERMANENT_SESSION_LIFETIME`. القيم الافتراضية لفلاسك لا تفرض إرسال الكوكي عبر HTTPS فقط، ولا تحدد سياسة SameSite بشكل صريح، والجلسات لا تنتهي صلاحيتها أبدًا — كوكي جلسة مسروق (عبر XSS على موقع آخر، أو شبكة غير مشفّرة) يبقى صالحًا إلى أجل غير مسمى.

**التوصية:** ضبط `SESSION_COOKIE_SECURE=True`، `SESSION_COOKIE_HTTPONLY=True`، `SESSION_COOKIE_SAMESITE='Lax'`، و`PERMANENT_SESSION_LIFETIME` بقيمة معقولة (ساعة إلى بضع ساعات).

### 9. مستخدم أدمن افتراضي بكلمة مرور معروفة عامة
`services/auth_service.py: seed_default_admin()` ينشئ مستخدم `admin` بكلمة مرور ثابتة `admin123` عند أول تشغيل إذا كان جدول المستخدمين فارغًا. إذا نُشر النظام دون تغيير هذه الكلمة فورًا، فهذه ثغرة دخول مباشرة معروفة للعامة (موثّقة أيضًا في الكود نفسه).

**التوصية:** توليد كلمة مرور عشوائية عند أول seed وطباعتها في اللوج مرة واحدة فقط (أو إجبار تغييرها عند أول دخول)، بدل قيمة ثابتة معروفة.

---

## 🟢 منخفضة / معلومات (Low / Info)

### 10. تسجيل تصحيح (debug logging) يكشف تفاصيل مصادقة
`services/auth_service.py:22,25` يحتوي على أسطر `print()` غير مشروطة بأي علم بيئة تكشف وجود/عدم وجود اسم المستخدم، وحالة تفعيل الحساب، ونجاح/فشل التحقق من كلمة المرور — لا تسرّب كلمة المرور نفسها، لكنها تسهّل هجمات تعداد المستخدمين (user enumeration) وتُكتب دائمًا في لوجات الحاوية (stdout) بما في ذلك بيئة الإنتاج. بالمثل، `fastapi_app/routes/auth.py:173` يسجّل اسم المستخدم (وليس كلمة المرور) عند فشل تسجيل دخول تركيب الجهاز — حساسية أقل لكنها تسهّل التعداد إذا تسرّبت اللوجات.

**التوصية:** استبدالها بـ `logger.debug()` مضبوط المستوى، وعدم تفعيله في الإنتاج.

### 11. لا تحقق من نطاق عناوين الـ mesh IP قبل الاتصال الصادر
أوامر الـ edge (`fastapi_app/services/controller/command_service.py`) تبني عنوان الاتصال كـ `http://{peer_ip}:8765/...` حيث `peer_ip` يأتي من استجابة NetBird API مباشرة، دون التحقق من كونه ضمن نطاق شبكة الـ mesh المتوقع (مثل `100.64.0.0/10`). لا يوجد مسار استغلال مباشر حاليًا (المستخدم لا يتحكم في القيمة)، لكنها فجوة دفاع بالعمق (defense-in-depth) في حال تعرّض استجابة NetBird نفسها للتلاعب.

**التوصية:** إضافة تحقق صريح من نطاق IP قبل أي طلب صادر لعنوان peer.

### 12. مسارات ملفات مؤقتة مبنية من معرّفات دون تعقيم صريح
`utils/cache_manager.py` يبني مسارات مثل `f"/tmp/peer_name_{peer_id}.json"` من معرّفات (`peer_id`, `customer_id`) دون تعقيم صريح. غير قابل للاستغلال حاليًا لأن مُحوِّل مسار Flask الافتراضي يرفض `/` داخل قطعة المسار في نقاط الاستدعاء الحالية، لكنه نمط هش لو تغيّر مصدر القيمة مستقبلًا.

### 13. تسريب طفيف لتفاصيل خطأ من جهاز الـ edge
`fastapi_app/services/controller/command_service.py` (حوالي الأسطر 226-236) يمرّر حقل `detail` من استجابة خطأ جهاز الـ edge مباشرة كـ `error` في استجابة الـ API. جهاز الـ edge مكوّن داخلي موثوق (داخل شبكة الـ mesh فقط)، لذا الخطورة منخفضة، لكنها تخالف قاعدة المشروع الخاصة بعدم تسريب تفاصيل داخلية في رسائل المستخدم (`.agents/rules/messages_and_validation.md`).

### 14. مجلدا `middleware/` و`websocket/` فارغان تمامًا — كود عظمي غير مفعّل
كل الملفات الأربعة في `middleware/` (`auth.py`, `rate_limit.py`, `error_handler.py`, `permissions.py`) وكلا الملفين في `websocket/` (`events.py`, `manager.py`) هي ملفات فارغة تمامًا (صفر بايت)، وغير مستوردة من أي مكان في التطبيق الفعلي. ليست ثغرة بحد ذاتها، لكن أسماء الملفات (`auth.py`, `permissions.py`, `rate_limit.py`) توحي بضوابط أمان *مخطَّط لها* — يستحق التأكيد الصريح مع الفريق أن هذه الوظائف مُغطّاة فعليًا في مكان آخر (كما هو مؤكد في هذا التقرير عبر آليات أخرى) وليست مجرد أسماء ملفات تعطي انطباعًا زائفًا بالأمان (false sense of security).

### 15. لا يوجد إعداد CORS في أي مكان
لا توجد أي إضافة `CORSMiddleware` أو رأس `Access-Control-Allow-Origin` في كامل الكود. ليست ثغرة (لا يوجد نمط "wildcard origin + credentials" الخطير لأنه لا توجد سياسة CORS من الأساس)، لكنها معلومة يجب مراعاتها عند أي تكامل واجهة أمامية من نطاق مختلف مستقبلًا.

---

## نقاط إيجابية مؤكدة

- **لا يوجد `.env` مرفوع على git** حاليًا أو في تاريخ الـ commits — مُدرَج بشكل صحيح في `.gitignore`.
- **لا توجد ثغرات حقن SQL**: كل استعلامات SQL الخام (`db.execute(text(...))`) تستخدم معاملات مقيّدة (`:param` + dict)، ولا يوجد أي بناء استعلام عبر f-string أو `.format()` أو `%` بمدخلات غير موثوقة — بما في ذلك نقطتا `db.execute()` الموجودتان في المسار العام غير المحمي بتوكن (`fastapi_app/routes/auth.py`).
- **لا يوجد حقن أوامر/كود**: لا `eval`/`exec`/`pickle.loads`/`yaml.load` غير آمن في أي مكان بالكود المتتبّع؛ الاستخدام الوحيد لـ `subprocess` هو في سكريبت تدقيق مسارات تطويري (`check_routes.py`) بدون `shell=True` وبدون مدخلات خارجية.
- **الصلاحيات بين المستأجرين (IDOR) مُطبَّقة باتساق**: كل المسارات الخاصة بجهاز/عميل في `client/routes/` تمرّ عبر `verify_peer_access()` التي تتحقق من ملكية الـ peer لعميل الجلسة الحالية، وليس من معرّف يُرسله الطالب مباشرة.
- **لا يوجد XSS نشط**: موضع `|safe` الوحيد في القوالب (`client/templates/client_base.html`) يُغذّى بقيمة ثابتة من القالب نفسه، وليس من مُدخل مستخدم أو قاعدة بيانات.
- **لا وضع Debug مفعّل في الإنتاج**: `app.run(debug=False)` في كلا التطبيقين، والتشغيل الفعلي عبر Gunicorn/Supervisor أصلًا يتجاوز هذا المسار بالكامل.
- **لا يوجد mass assignment**: مخططات Pydantic ونماذج Flask لا تسمح بتمرير حقول حساسة (مثل `role`/`is_admin`) مباشرة من مدخلات العميل؛ حقل `role` في إدارة الموظفين محصور بقائمة مسموحة صريحة.
- **لا يوجد رفع ملفات أو تقديم ملفات ديناميكي غير آمن**: لا استخدام لـ `send_file`/`send_from_directory`/`request.files` في أي مكان بالتطبيق — لا مسار استغلال traversal لأنه لا يوجد المسار أصلًا.
- **ملفات الـ migrations نظيفة**: لا أسرار أو بيانات seed حساسة، ولا استعلامات SQL مبنية من مدخلات غير موثوقة.
- **لا أسرار في `.claude/`**: يحتوي فقط على ملف قفل تشغيلي (`scheduled_tasks.lock`) بدون بيانات حساسة.
- الاعتمادات الأساسية (Flask 3.1.3، FastAPI 0.115.0، SQLAlchemy 2.0.51، Jinja2 3.1.6، pydantic 2.9.2) حديثة نسبيًا ولا تحمل ثغرات معروفة واضحة في هذه الإصدارات. القائمة الكاملة للاعتمادات موثّقة في `requirements.txt` (ملاحظة فنية بسيطة: الملف مُرمَّز UTF-16 مع BOM، غير معتاد لكنه ليس مشكلة أمنية).

---

## أولويات الإصلاح المقترحة (بالترتيب)

1. تدوير مفتاح `INTERNAL_API_KEY`/NetBird فورًا، وحذف كل نسخه الثابتة من الملفات التسعة المذكورة (بما فيها `services/subscription_service.py`)، والاعتماد الحصري على متغير البيئة.
2. تغيير كلمة مرور بريد `no_reply@networkat.net` وحذف القيمة الثابتة من كلا الموضعين (`config/settings.py` و`utils/email.py`).
3. تفعيل `CSRFProtect` على تطبيقي Flask.
4. الانتقال لـ bcrypt الفعلي في `utils/password.py` (المكتبة موجودة أصلًا).
5. إضافة Rate Limiting على `/login` (كلا التطبيقين) وعلى مسارات التحقق بالكود (OTP).
6. استبدال `random.randint` بـ `secrets` لتوليد أكواد التحقق، وإضافة تحقق صيغة البريد قبل استخدامه (`email-validator`).
7. ضبط إعدادات كوكي الجلسة (`SECURE`, `SAMESITE`, `PERMANENT_SESSION_LIFETIME`).
8. تغيير آلية إنشاء الأدمن الافتراضي بحيث لا تُنتج كلمة مرور معروفة مسبقًا.
9. توثيق استثناء `/scripts` العام بوضوح، ومراجعة أي ملف يُضاف إليه.

---

*تم إعداد هذا التقرير عبر فحص يدوي وآلي (grep موجّه) لكامل الكود المتتبّع في `core/web_app/` بتاريخ 2026-09-15. لا يغطي هذا التقرير: تكوين البنية التحتية خارج هذا الـ repo (`core/`, `netbird/`)، فحص الاعتمادات عبر أداة SCA مخصصة (مثل `pip-audit`)، أو اختبار اختراق فعلي (dynamic testing) على بيئة تشغيل حقيقية.*
